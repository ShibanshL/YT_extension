import React from "react";
import ReactDOM from "react-dom/client";
import First_load from "./First_Load";

ReactDOM.createRoot(document.getElementById("firstLoad-root")!).render(
  <React.StrictMode>
    <First_load />
  </React.StrictMode>
);
